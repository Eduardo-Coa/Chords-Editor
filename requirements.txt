# Dependencias de runtime de HymnChords.
#
# La UI usa CustomTkinter para la "cáscara" (ventanas, listas, barras, popups y
# áreas con scroll). El widget chord_grid se mantiene en tkinter puro (stdlib)
# por rendimiento. La lógica (models/, utils/) NO depende de customtkinter.
# darkdetect lo instala customtkinter automáticamente.
#
# Instalar:  pip install -r requirements.txt
customtkinter>=5.2.2
